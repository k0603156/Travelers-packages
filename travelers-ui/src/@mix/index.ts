const base_tyle = `
    outline: none;
    border: none;
    border-radius: 0.25rem;
    box-sizing: border-box;
    &:focus {
        box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.2);
    }
`;
export default { base_tyle };
